# Coordinated SLD, Measurement, Protection, and LineTool Remediation Implementation Plan\n\n> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.\n\n**Goal:** Correct the five coordinated residual architectural workstreams in GridForge V2 using one canonical endpoint identity, an explicit measurement provisioning boundary, preserved protection composition, the existing Application command path for LineTool, and one master audit register, with static verification only.\n\n**Architecture:** Core remains authoritative for electrical equipment, Core Terminals, measurement channels, topology, protection, studies, and results. UI snapping produces presentation metadata only; EndpointReference(equipment_type, equipment_id, terminal_role) is the sole semantic endpoint identity. Measurement provisioning consumes explicit endpoint references and creates logical channels from authoritative physical terminals, while protection consumes those channels through RelayInput. Real Line equipment creation remains an immutable Application command flow.\n\n**Tech Stack:** Python, existing GridForge Core/Application/SLD UI architecture, immutable dataclass/value-object contracts, existing GitHub repository structure.\n\n**Spec:** docs/superpowers/specs/2026-09-23-coordinated-sld-measurement-protection-remediation-design.md\n\n## Global Constraints\n\n- Core owns electrical equipment, authoritative Core Terminal objects, topology, measurement, protection, studies, and results.\n- Application is the sole UI-to-Core mutation boundary.\n- UI, SLD, tools, renderers, and plugins remain presentation/interaction layers.\n- The canonical semantic endpoint identity is EndpointReference(equipment_type, equipment_id, terminal_role).\n- EquipmentTerminal.terminal_id is UI/document identity only and must not become a second semantic identity authority.\n- Terminal roles are explicit metadata; they are never inferred from geometry, index, or ordering.\n- No ProtectionTerminal or MeasurementTerminal abstraction is introduced.\n- No second topology, connection, equipment, or terminal identity authority is introduced.\n- Tests, CI, startup, GUI execution, and runtime smoke verification are prohibited in this phase.\n- Runtime verification must remain UNVERIFIED / DEFERRED.\n- Do not reopen already-corrected adjacent components unless the dependency trace proves a direct contract violation.\n\n## Review Focus\n\n1. A bus snap must carry a presentation-level object identity without inventing a Core terminal.\n2. A multi-terminal equipment snap must preserve explicit terminal role and reject ambiguous terminal metadata rather than infer it.\n3. CT/PT/CVT provisioning must preserve one canonical source-terminal identity and must not create duplicate terminal maps.\n4. Protection composition must consume existing MeasurementChannel objects and must not generate them inside ProtectionRuntime.\n5. LineTool must create real Core Line equipment only through the existing immutable Application command path and must not become a second topology authority.\n\n---\n\n### Task 1: Establish baseline and dependency map\n\n**Files:**\n- Inspect only: ui/items/bus_item.py\n- Inspect only: ui/core/snap_system.py\n- Inspect only: ui/items/equipment_item.py\n- Inspect only: ui/tools/endpoint_identity_adapter.py\n- Inspect only: core/application/endpoint_reference.py\n- Inspect only: core/application/endpoint_resolver.py\n- Inspect only: core/measurement/measurement_channel.py\n- Inspect only: core/measurement/measurement_generation.py\n- Inspect only: core/application/commands/measurement_commands.py\n- Inspect only: core/application/commands/model_commands.py\n- Inspect only: core/application/commands/connection_commands.py\n- Inspect only: core/protection/protection_measurement_binding.py\n- Inspect only: core/protection/relay_input.py\n- Inspect only: core/protection/runtime.py\n- Inspect only: core/protection/protection_element.py\n- Inspect only: core/protection/protection_system.py\n- Inspect only: ui/tools/line_tool.py\n- Inspect only: core/application/application.py\n- Inspect only: core/application/command_manager.py\n- Inspect only: core/application/command_handlers.py\n- Inspect only: audit/MASTER_AUDIT_REGISTER.md\n- Inspect only: audit/MASTER_AUDIT_REGISTER.csv\n\n**Interfaces:**\n- Consumes: current main source state and approved design specification.\n- Produces: a file-level dependency map used by every subsequent task.\n\n- [ ] Step 1: Record current HEAD and identify every participating file. Confirm that the implementation base is the current main tip containing the approved design commit. Do not execute application code.\n- [ ] Step 2: Trace endpoint identity definitions and consumers. Record the exact EndpointReference factories and all inspected callers before editing. Treat the existing endpoint resolver as protected architecture unless the trace identifies a direct defect.\n- [ ] Step 3: Trace measurement and protection constructors/registrations. Record MeasurementChannel constructor parameters, source-terminal setters, measurement-generation inputs, channel registries, RelayInput construction, binding construction, and ProtectionRuntime.compose().\n- [ ] Step 4: Trace LineTool command execution. Confirm LineTool imports CreateLineCommand, captures endpoint references through EndpointIdentityAdapter, and delegates execution through ToolBase.execute_command(); identify the exact Application/CommandManager continuation.\n- [ ] Step 5: Identify the existing authoritative register rows. Locate the requested finding IDs in audit/MASTER_AUDIT_REGISTER.md and audit/MASTER_AUDIT_REGISTER.csv; do not create a second register.\n- [ ] Step 6: Produce a static dependency map in the implementation notes. The map must explicitly list changed files, protected unchanged files, identity flow, measurement flow, protection flow, LineTool flow, and register rows.\n\n### Task 2: Add the Bus presentation snap-candidate contract\n\n**Files:**\n- Modify: ui/items/bus_item.py\n- Inspect: ui/items/equipment_item.py\n- Inspect: ui/core/snap_system.py\n\n**Interfaces:**\n- Consumes: existing BusItem graphical position/radius and SnapSystem candidate contract.\n- Produces: BusItem.snap_points() -> iterable containing a scene-space graphical candidate with explicit object identity and no Core mutation.\n\n- [ ] Step 1: Define the minimal BusItem candidate shape. Return the existing graphical anchor represented in scene coordinates and carry object_id=self.object_id. Do not add Terminal, terminal role, topology, or equipment lookup to BusItem.\n- [ ] Step 2: Preserve the existing BusItem presentation boundary. Keep painting, movement, selection, and radius behavior unchanged. The new method must be a pure presentation query.\n- [ ] Step 3: Reconcile SnapSystem normalization with BusItem output. Ensure a candidate supplied as a mapping preserves object_id, terminal_id, and terminal_name when explicitly present. Do not populate terminal fields from geometry or candidate ordering.\n- [ ] Step 4: Re-audit the object-snap result contract statically. Confirm SnapResult remains immutable and that the normalized candidate retains its source object and explicit terminal metadata through the result.\n\n### Task 3: Make terminal-aware snapping canonical\n\n**Files:**\n- Modify: ui/core/snap_system.py\n- Modify: ui/tools/endpoint_identity_adapter.py\n- Inspect: ui/items/equipment_item.py\n- Inspect: equipment terminal/symbol-definition modules participating in EquipmentItem.snap_points()\n- Inspect: core/application/endpoint_reference.py\n\n**Interfaces:**\n- Consumes: existing EquipmentTerminal presentation metadata and canonical EndpointReference.\n- Produces: deterministic SnapResult metadata and canonical endpoint conversion.\n\n- [ ] Step 1: Preserve explicit terminal metadata during normalization. Keep terminal_id as UI/document identity only. Preserve terminal_name/role exactly when explicitly supplied.\n- [ ] Step 2: Reject incomplete terminal candidates at the identity boundary. If a terminal snap lacks the explicit terminal role required to construct a canonical endpoint, fail deterministically rather than using geometry, index, ordering, or terminal_id as a semantic substitute.\n- [ ] Step 3: Remove the duplicate CT/PT/CVT alias map from EndpointIdentityAdapter. Reuse the repository's canonical EquipmentType normalization/factory. Do not add another semantic equipment mapping.\n- [ ] Step 4: Make the adapter reuse canonical endpoint references when already present. If a source item supplies an EndpointReference, return that canonical value rather than reconstructing a parallel identity.\n- [ ] Step 5: Statically trace both required identity chains. Verify BusItem → SnapSystem → SnapResult → EndpointIdentityAdapter → EndpointReference.bus(). Verify SymbolDefinition → EquipmentDefinition → EquipmentTerminal → EquipmentItem.snap_points() → SnapResult → EndpointReference.terminal().\n\n### Task 4: Introduce the smallest measurement provisioning boundary\n\n**Files:**\n- Create: core/measurement/measurement_provisioning.py\n- Modify: core/measurement/measurement_generation.py\n- Modify: core/measurement/measurement_channel.py only if the source-terminal contract is inconsistent after the constructor/setter audit\n- Inspect: core/application/endpoint_reference.py\n- Inspect: existing CT/PT/CVT Core model modules and their authoritative Terminal collections\n- Inspect: core/application/commands/measurement_commands.py\n- Inspect: existing measurement registries/consumers\n\n**Interfaces:**\n- Consumes: explicit EndpointReference for the physical source terminal, an authoritative Core terminal resolver/lookup, instrument object, channel definition, and existing MeasurementGeneration.\n- Produces: registered MeasurementChannel instances whose source-terminal identity remains an EndpointReference.\n\n- [ ] Step 1: Define the provisioning boundary around channel creation/registration. The provisioning service accepts an explicit terminal EndpointReference; it does not infer source identity from class name, terminal index, list order, channel name, or geometry.\n- [ ] Step 2: Resolve the endpoint against authoritative Core state. Use the existing canonical endpoint resolver rather than creating a second terminal lookup table.\n- [ ] Step 3: Provision CT/PT/CVT secondary channels without new terminal abstractions. Secondary terminals remain Core Terminal objects. The new boundary may orchestrate them but must not own an alternate terminal registry.\n- [ ] Step 4: Reconcile MeasurementChannel construction and update paths. Ensure constructor, set_source_terminal(), update/reset, diagnostics, and source-terminal accessors all preserve the same explicit source identity contract.\n- [ ] Step 5: Align MeasurementGeneration with explicit endpoint provenance. Where PreparedMeasurementContext currently carries a string source-terminal value, change the boundary to consume the canonical endpoint reference or an immutable context field derived directly from it, without introducing another semantic identity type.\n- [ ] Step 6: Audit duplicate CT/PT/CVT channel registration. Confirm there is one authoritative channel registration/keying mechanism and that duplicate channel identity is rejected or deterministically prevented.\n- [ ] Step 7: Statically verify independent secondary terminals. Confirm S1/S2 and X1/X2/H1/H2-style secondary terminals remain independent Core Terminal objects and are represented by explicit terminal roles rather than positional inference.\n\n### Task 5: Reconcile ProtectionMeasurementBinding integration\n\n**Files:**\n- Modify: core/protection/protection_measurement_binding.py if required by the provenance audit\n- Inspect: core/protection/relay_input.py\n- Inspect: core/protection/protection_element.py\n- Inspect: core/protection/protection_system.py\n- Inspect: core/protection/runtime.py\n- Inspect: measurement provisioning/generation modules from Task 4\n\n**Interfaces:**\n- Consumes: provisioned MeasurementChannel and explicit source-terminal EndpointReference.\n- Produces: immutable ProtectionMeasurementBinding retaining source-terminal identity while RelayInput consumes the authoritative channel.\n\n- [ ] Step 1: Preserve EndpointReference as the binding source identity. Keep source_terminal_reference: EndpointReference as the canonical provenance field and reject non-terminal references.\n- [ ] Step 2: Remove any remaining semantic inference in binding construction. Source equipment type and terminal role must come from the explicit terminal reference or authoritative terminal contract, never from names, indexes, or ordering.\n- [ ] Step 3: Verify RelayInput remains a live reference to MeasurementChannel. Do not copy measurement values or create a new measurement abstraction.\n- [ ] Step 4: Verify ProtectionRuntime only composes protection. ProtectionRuntime.compose() may consume a mapping of existing channels and create RelayInputs/ProtectionElements, but must not instantiate physical instruments or generate measurement channels.\n- [ ] Step 5: Trace the complete protection dependency chain statically. Verify physical terminal → EndpointReference → measurement provisioning → MeasurementChannel → RelayInput → ProtectionElement → ProtectionSystem → ProtectionDecision.\n\n### Task 6: Reconcile LineTool against the real Core Line workflow\n\n**Files:**\n- Inspect/modify: ui/tools/line_tool.py\n- Inspect: core/application/commands/model_commands.py\n- Inspect: core/application/application.py\n- Inspect: core/application/command_manager.py\n- Inspect: core/application/command_handlers.py\n- Inspect: Line command handler/service and Line Core model modules\n- Inspect: ui/tools/tool_base.py\n\n**Interfaces:**\n- Consumes: two canonical endpoint references and UI engineering parameters.\n- Produces: immutable CreateLineCommand executed through Application.\n\n- [ ] Step 1: Confirm LineTool's current role from implementation. The current implementation imports CreateLineCommand, captures endpoint references, and requires engineering line parameters. Treat this as evidence that it represents real Core Line equipment unless a call-site audit disproves it.\n- [ ] Step 2: Verify command immutability and payload identity. Confirm CreateLineCommand carries canonical endpoint references and engineering values without direct Core object references.\n- [ ] Step 3: Verify ToolBase execution boundary. Confirm execute_command() routes to Application execution rather than mutating Core directly.\n- [ ] Step 4: Correct only any discovered shortcut. If the static call graph contains a direct Core mutation or alternate topology path, replace that shortcut with the existing Application command route. Do not add a second connection manager or topology service.\n- [ ] Step 5: Reconcile preview-only behavior separately. Keep transient graphical preview state in LineTool/UI only; the preview must not become a Core Line or topology mutation.\n- [ ] Step 6: Trace the complete command path. Verify LineTool → immutable CreateLineCommand → Application.execute() → CommandManager → handler/service → Core → semantic event → ReadModel → SLD projection.\n\n### Task 7: Update the existing master audit register\n\n**Files:**\n- Modify: audit/MASTER_AUDIT_REGISTER.md\n- Modify: audit/MASTER_AUDIT_REGISTER.csv\n\n**Interfaces:**\n- Consumes: static findings and evidence from Tasks 1–6.\n- Produces: one authoritative register with synchronized Markdown/CSV records.\n\n- [ ] Step 1: Update only the requested finding rows. Cover GF-SLD-TERM-020, GF-SLD-TERM-021, GF-SLD-TERM-023–GF-SLD-TERM-028, GF-SLD-SNAP-022, RCA-SLD-CONN-002, GF-MASTER-0067, GF-PROT-035–GF-PROT-040, GF-PROT-042, and GF-SLD-WF-TOOL-006.\n- [ ] Step 2: Record root cause and affected modules. Each row must identify the observed contract failure and the concrete source modules participating in its dependency chain.\n- [ ] Step 3: Record architectural impact and minimal remediation. State which authority boundary was violated and which existing authority is reused after correction.\n- [ ] Step 4: Record verification state explicitly. Static verification must describe the source/call-graph evidence. Runtime verification must remain UNVERIFIED / DEFERRED.\n- [ ] Step 5: Synchronize Markdown and CSV. Ensure the same finding IDs, status language, root cause, and verification state are represented in both existing register formats.\n\n### Task 8: Perform the complete static integrity re-audit\n\n**Files:**\n- Inspect: all files modified in Tasks 2–7\n- Inspect: all dependency-chain endpoints identified in Task 1\n- Inspect: audit/MASTER_AUDIT_REGISTER.md\n- Inspect: audit/MASTER_AUDIT_REGISTER.csv\n\n**Interfaces:**\n- Consumes: completed source corrections.\n- Produces: static-only closure evidence and a final audit state.\n\n- [ ] Step 1: Re-run static identity-flow inspection. Verify both Bus and generic terminal endpoint chains end at canonical EndpointReference factories.\n- [ ] Step 2: Re-run static authority inspection. Confirm no UI-to-Core mutation shortcut, second topology authority, second equipment identity authority, or second terminal identity authority was introduced.\n- [ ] Step 3: Re-run terminal-role integrity inspection. Search changed dependency paths for role derivation from geometry, index, candidate order, or terminal_id.\n- [ ] Step 4: Re-run measurement integrity inspection. Confirm one provisioning boundary, one channel registration authority, explicit endpoint source identity, consistent channel construction/setter/update paths, and independent Core secondary terminals.\n- [ ] Step 5: Re-run protection integrity inspection. Confirm RelayInput consumes MeasurementChannel, binding retains EndpointReference, and ProtectionRuntime remains composition-only.\n- [ ] Step 6: Re-run LineTool integrity inspection. Confirm concrete Line creation uses immutable command/Application execution and no alternate topology path exists.\n- [ ] Step 7: Reconcile the complete requested finding set. Every requested ID must have a corresponding register update and static evidence chain. Do not call a finding closed merely because a file was edited.\n- [ ] Step 8: Record verification boundary. Final status must distinguish STATICALLY VERIFIED from RUNTIME VERIFICATION DEFERRED. No tests, CI, startup, GUI execution, or runtime smoke checks are performed.\n- [ ] Step 9: Commit the completed remediation as a coherent change set. Use a focused commit message describing the coordinated SLD/measurement/protection/LineTool correction after the static re-audit is complete.